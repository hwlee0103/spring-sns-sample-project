# Code Review History — 2026-04

## Review #1

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 1 | Critical | UserService.java | :14 | `register()`에서 `existsByEmail`과 `save` 사이 race condition — 동시 요청 시 중복 저장 가능. `DataIntegrityViolationException` 미처리로 500 에러 발생 | 검증 및 소스 재 리뷰 완료 | 2026-04-09 |
| 2 | Critical | User.java | :33 | 비밀번호 평문 저장 — Argon2 암호화 적용 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 3 | Warning | User.java | :37-39 | `update()` 메서드에서 nickname, password에 null/빈 문자열 검증 없음 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 4 | Warning | UserCreateRequest.java | :5 | 요청 DTO에 `@NotBlank` 등 Bean Validation 미적용 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 5 | Warning | UserUpdateRequest.java | :3 | 요청 DTO에 Bean Validation 미적용 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 6 | Warning | GlobalExceptionHandler.java | :12-15 | `UserException`만 처리 — 공통 부모 예외 클래스 도입 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 7 | Suggestion | UserRepository.java | :8 | `findByEmail()` 미사용 메서드 — 불필요 시 제거 고려 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 8 | Suggestion | UserController.java | :39-42 | `getUsers()` 전체 조회 — 페이징(`Pageable`) 도입 고려 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |

## Review #2

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 9 | Warning | User.java | :10,38-40 | Entity가 `PasswordEncoder`(Spring Security)를 직접 의존 — 도메인 레이어 독립성 저해. Service에서 인코딩 후 값을 전달하는 방식으로 변경 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 10 | Warning | UserService.java | :21,40 | `register()`는 Entity에 위임, `update()`는 Service에서 직접 인코딩 — 암호화 패턴 비일관. update()는 Service 직접 인코딩으로 통일되었으나 register()는 여전히 Entity 위임 방식 유지 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |

## Review #3

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 11 | Warning | UserService.java | :18-28 | `register()` 에 raw 값(email/password/nickname) Service 레벨 방어 검증 없음. Bean Validation 우회 경로(내부 호출)에서 NPE 위험. `update()` 처럼 인코딩/저장 호출 전 raw 값 선검증 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 12 | Warning | User.java | :32-36 | `public User(String, String, String)` 생성자에서 raw 파라미터 null/blank 검증 없이 필드 저장 — Entity 불변식 방어 누락. 생성자에서도 검증 적용 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 13 | Warning | UserService.java | :48-53 | `delete()` 가 `existsById` + `deleteById` 로 DB 2회 접근. 트랜잭션 미적용으로 두 호출 사이 race condition 가능. `findById().ifPresent(repo::delete)` 또는 `@Transactional` 적용 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 14 | Suggestion | GlobalExceptionHandler.java | :14-17 | 모든 `DomainException` 을 400(Bad Request)로 응답. `notFound` 류는 의미상 404 가 적절. `DomainException` 에 HTTP 상태 노출 또는 `NotFoundException` 하위 타입 도입 검토 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 15 | Suggestion | UserException.java | :7-9 | `public UserException(String)` 생성자가 외부에 노출. 정적 팩토리만 허용하도록 생성자를 `private` 로 제한하여 메시지 포맷 일관성 강제 가능 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 16 | Suggestion | UserController.java | :51-56 | `updateUser` 가 DTO 필드를 풀어 service에 전달. `UserUpdateRequest` 에 `toCommand()` / VO 변환 메서드를 두어 Controller-Service 결합도 완화 검토 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 17 | Suggestion | DomainException.java | :6 | `serialVersionUID` 미정의 — `RuntimeException` 직렬화 경고 가능성. 명시 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 18 | Suggestion | PageResponse.java | :7-25 | `content` 가 가변 `List<T>` 로 노출됨. `List.copyOf(...)` 로 감싸 불변성 보장 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |

## Review #4

> 백엔드 인증/Post 도메인 + 프론트엔드 Next.js 16 풀스택 부트스트랩 직후 리뷰. 4월 기존 이슈(#9~#18)는 모두 미해결 상태로 변동 없음.

### Critical (must fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 19 | Critical | SecurityConfig.java | :21-54 | 로그인 시 **세션 고정 공격(Session Fixation) 방어 누락**. `AuthController` 가 수동으로 `authenticate()` + `securityContextRepository.saveContext()` 를 호출하는데, 세션 ID 교체(`request.changeSessionId()` 또는 `SessionAuthenticationStrategy.onAuthentication`)를 수행하지 않음. 로그인 전후 세션 ID 동일 → 공격 가능 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 20 | Critical | SecurityConfig.java | :22 | **CSRF 비활성화 + 쿠키 세션 조합으로 CSRF 공격에 노출**. same-origin 프록시 구조에서 제3자 사이트의 폼 제출도 쿠키가 자동 첨부됨. 최소 `SameSite=Strict` 강제 또는 `CookieCsrfTokenRepository` 도입 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 21 | Critical | PostController.java | :32-34, 52-57, 61-64 | `Authentication` 파라미터 null 방어 없음 → 룰 변경/anonymous 활성화 시 NPE 직행. 추가로 `authentication.getName()` → Service 가 `userService.getByEmail()` 로 1회 더 DB 조회 → 모든 mutation 에서 불필요한 추가 쿼리. `@AuthenticationPrincipal` 로 사용자 ID 직접 주입 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 22 | Critical | frontend/lib/api.ts | :155-165, 178-189 | **에러/스키마 검증 실패 시 response body stream 누수**. `parseError` 의 `response.json()` 실패 catch 경로에서 body 를 텍스트로 fallback 소진하지 않음. `response.clone()` 후 json 시도 + 실패 시 `response.text()` 로 stream 확실히 소진 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 23 | Critical | frontend/features/post/hooks/useFeedQuery.ts, components/FeedList.tsx | useFeedQuery.ts:7, FeedList.tsx:19-35 | (a) `feedQueryKey = ['feed']` 단일 키 → 정렬/사이즈 변경 시 캐시 오염. (b) `useCreatePostMutation` 의 전체 invalidate → 무한쿼리 모든 페이지 refetch 로 네트워크 과부하. (c) IntersectionObserver effect 의 `isFetchingNextPage` deps → 페치 토글마다 disconnect/re-observe 로 연속 스크롤 끊김 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |

### Warning (should fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 24 | Warning | AuthController.java | :49-51 | `BadCredentialsException` 만 401 변환. `LockedException`, `DisabledException`, `AccountExpiredException`, `UsernameNotFoundException` 등 다른 `AuthenticationException` 하위는 500 으로 떨어짐. 공통 부모 `AuthenticationException` catch 또는 `GlobalExceptionHandler` 핸들러 추가 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 25 | Warning | AuthController.java | :54-63 | `me()` 가 `getByEmail` 시 사용자가 그 사이 삭제되면 `UserException.notFoundByEmail` → 400 응답. 401 매핑 또는 세션 정리 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 26 | Warning | PostService.java | :32-50 | `update()` 만 `@Transactional` 이고 `delete()` 는 미적용. `delete()` 는 getById + getByEmail + delete 로 3회 DB 접근이 각각 독립 트랜잭션. 단일 트랜잭션으로 묶어야 함 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 27 | Warning | PostRepository.java | :9 | `findAllByOrderByIdDesc` 메서드명에 정렬을 하드코딩 → Controller 가 `@PageableDefault(sort=...)` 로 변경해도 무시됨. 표준 `findAll(Pageable)` 사용으로 충분 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 28 | Warning | PostException.java | :7 | 생성자가 `public` — 이슈 #15 와 동일 패턴이 새 도메인에 전파. 정적 팩토리만 허용하도록 `private` 제한 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 29 | Warning | SecurityConfig.java | :31-32 | `/api/auth/me` 를 permitAll 로 두고 컨트롤러에서 anonymous 수동 체크. `authenticated()` + `AuthenticationEntryPoint` 401 반환이 표준. 현재 방식은 일관성 부족 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 30 | Warning | UserDetailsServiceImpl.java | :18-28 | 존재하지 않는 이메일에 대해 `passwordEncoder.matches` 없이 즉시 throw → **타이밍 공격으로 계정 존재 여부 탐지 가능**. 더미 hash 와 matches 호출로 타이밍 균일화 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 31 | Warning | frontend/lib/api.ts | :129-189 | 제네릭 반환 타입에 zod 의 `infer Out` 사용 → zod v4 의 3-제네릭 ZodType 에서 추론 실패 가능. `z.output<NonNullable<TSchema>>` 사용 권장 (특히 `transform` 결과 타입의 정확성) | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 32 | Warning | frontend/lib/api.ts | :155-160 | `AbortError` 도 `ApiError(status=0)` 로 래핑 → TanStack Query 의 cancel/언마운트 흐름과 구분 불가. `e.name === 'AbortError'` 는 rethrow 해야 함 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 33 | Warning | frontend/lib/api.ts | :186 | 스키마 검증 실패 시 `status = response.status`(=200)로 `ApiError` 생성 → `isUnauthorized` 등 플래그와 충돌 여지. 별도 에러 클래스 또는 `status: 0` 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 34 | Warning | frontend/components/providers/QueryProvider.tsx | :14 | `getQueryClient()` 를 컴포넌트 내부 직접 호출. React strict mode/동시 렌더에서 클라이언트 인스턴스 흔들림 위험. `useState(() => getQueryClient())` 패턴 권장 (TanStack Query SSR 가이드) | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 35 | Warning | frontend/features/auth/components/LoginForm.tsx, SignupForm.tsx, frontend/features/post/components/PostComposer.tsx | LoginForm:45-49, SignupForm:54-58, PostComposer:43-47 | `setError(field as keyof Values)` 강제 캐스팅 — 백엔드가 알 수 없는 필드명을 응답하면 react-hook-form 이 silent fail 하여 사용자가 에러를 못 봄. 화이트리스트 매핑 + 미지원 필드는 `setError('root', ...)` 로 fallback 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 36 | Warning | frontend/features/auth/components/SignupForm.tsx | :46-52 | 가입 → 자동 로그인 연쇄 호출. 가입 성공 후 로그인만 실패 시 사용자 혼란. 가입 409 에러와 로그인 401 에러가 동일 필드로 매핑될 여지. 단계 분리 + 메시지 차별화 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 37 | Warning | frontend/components/layout/Header.tsx | :20-41 | 초기 렌더 시 `useCurrentUser` 가 fetch 완료 전까지 비로그인 UI 가 깜빡일 가능성. `HydrationBoundary` 로 prefetch 전달하여 SSR 시점에 상태 결정 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 38 | Warning | frontend/features/post/components/FeedItem.tsx | :8-15, 17 | `'use client'` 선언 없음. Server Component 로 직접 렌더되면 `Date.now()` 가 SSR 시각 → hydration mismatch ("방금 전" → "2분 전"). 파일 단위 보장 위해 `'use client'` 명시 또는 `suppressHydrationWarning` 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 39 | Warning | frontend/features/post/components/ComposerSection.tsx, components/layout/Header.tsx | ComposerSection:10, Header:10 | 동일 `useCurrentUser` 두 곳에서 호출 — staleTime 만료 후 중복 fetch 가능. TanStack Query dedupe 정상 동작으로 실해 없음, 수용 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 40 | Warning | frontend/lib/env.ts | :24-27 | 모듈 로드 시점에 `throw` → 잘못된 환경변수 시 Next.js 빌드/부팅 자체 실패. 의도적 fail-fast 설계로 수용 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 41 | Warning | SecurityConfig.java | :51 | `frameOptions.sameOrigin()` 를 H2 콘솔 위해 전역 허용 — 운영 환경에서 clickjacking 가능. dev profile 에서만 허용하도록 분리 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |

### Suggestion (consider improving)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 42 | Suggestion | frontend/features/post/types.ts | :14 | `createdAt: z.string().transform((s) => new Date(s))` 가 유효성 검사 없이 Date 생성 → `Invalid Date` 가 통과. `z.string().datetime()` 선행 검증 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 43 | Suggestion | UserDetailsServiceImpl.java | 위치 | Spring Security 어댑터인 `UserDetailsServiceImpl` 이 `domain/user/` 에 위치. `domain/user/security/` 또는 `config/` 로 이동하여 레이어 의도 명확화 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 44 | Suggestion | frontend/features/post/components/PostComposer.tsx | :68-72 | 글자 수 카운터는 `content.length`, 게시 버튼 disable 은 `content.trim().length === 0` 사용 → 공백만 입력 시 카운터 표시되지만 버튼 비활성으로 UX 혼동 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |

## Review #6 (2026-04-12)

> 전체 소스 리뷰. 기존 미해결 14건 중 5건 해결 확인 (#18, #26, #33, #34, #38). 신규 4건 발견.

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 63 | Warning | ErrorType.java | :11 (domain/common) | `ErrorType` 이 `org.springframework.http.HttpStatus` 에 직접 의존하면서 `domain/common/` 에 위치 — Javadoc 의 "도메인이 HttpStatus 에 의존하지 않도록" 의도와 상충. `config/` 또는 `controller/` 이동 검토, 또는 도메인에 순수 분류만 두고 HTTP 매핑은 GlobalExceptionHandler 의 switch 로 처리 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 64 | Suggestion | PostComposer.tsx | :42-52 | LoginForm/SignupForm 은 `applyApiErrors()` 를 사용하지만 PostComposer 만 수동 `setError(field as keyof)` 유지. `applyApiErrors(setError, e, ['content'] as const)` 로 통일하면 #35 도 함께 해결 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 65 | Suggestion | UserUpdateCommand.java | :9 | `nickname`, `rawPassword` 필드에 어노테이션 없이 nullable 허용 구조. Service 에서 검증하므로 동작 문제 없으나, 도메인 command 라는 역할이 코드만으로는 불명확. 현재 Javadoc 으로 충분히 커버됨 — 수용 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 66 | Suggestion | SecurityConfig.java | :43 | `CsrfCookieFilter` 가 `new` 인스턴스로 등록. 의존성 주입 불필요하므로 현재 문제 없으나, 향후 확장 시 @Bean 으로 관리하면 일관적 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |

## Review #7 — 해커 관점 보안 리뷰 (2026-04-13)

> 해커/침투 테스터 관점에서 인증·인가·세션·rate limiting 을 중심으로 공격 시나리오 기반 리뷰.

### Critical / High (수정 완료)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 67 | Critical | UserController.java | :54-58 | **IDOR: `PUT /api/user/{id}` 본인 검증 없음** — 인증된 아무 사용자가 타인 프로필+비밀번호 덮어쓰기 가능. `curl -X PUT /api/user/1 -d '{"nickname":"pwned","password":"..."}' -b attacker_session` 으로 계정 탈취 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 68 | Critical | UserController.java | :73-77 | **IDOR: `DELETE /api/user/{id}` 본인 검증 없음** — 인증된 아무 사용자가 타인 계정 삭제 가능 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 69 | High | RateLimitFilter.java | :64-70 | **X-Forwarded-For 스푸핑으로 rate limit 무력화** — 공격자가 요청마다 다른 XFF 헤더를 보내 IP 기반 제한 우회. 무제한 brute force 가능 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 70 | High | RateLimitFilter.java | :30 | **Rate limit 맵 무한 증가 → OOM** — XFF 스푸핑으로 수백만 고유 키 생성 시 ConcurrentHashMap 메모리 고갈 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 71 | High | AuthController.java, UserController.java | me(), changePassword | **changePassword 후 현재 세션 stale** — tokenVersion bump 후 본인 세션의 AuthUser 가 구버전 유지 → 다음 me() 호출 시 본인이 로그아웃됨. 또한 공격자가 me() 를 피하면 탈취 세션이 계속 유효 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |

### Medium (미수정 — 인지/수용/후속 처리)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 72 | Medium | UserException.java | emailAlreadyExists | **회원가입 시 이메일 존재 여부 노출 (409)** — 에러 메시지에서 email 값 제거 완료. 메시지 자체의 분리(email vs nickname)는 #125 참조 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 73 | Medium | application.yaml | dev profile | **dev 프로필 cookie 에 secure/http-only/same-site 미설정** — #128로 통합 해결 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 74 | Medium | SecurityConfig.java | H2 CSRF ignore | **H2 콘솔 CSRF 면제가 프로필 무관** — `/h2-console/**` CSRF ignore 는 dev 프로필 조건과 연동되지 않아 prod 에서도 면제됨 (다만 접근 자체는 dev only 로 차단됨) | detect | |
| 75 | Medium | UserUpdateRequest.java | password 필드 | **프로필 수정(nickname) 과 비밀번호 변경이 결합** — `PUT /api/user/{id}` 가 항상 password 를 요구하여 공격 표면 확대. 프로필 수정은 nickname 만, 비밀번호는 `/password` 엔드포인트 전용으로 분리 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |

### Low / Informational (미수정 — 수용)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 76 | Low | User.java | tokenVersion | **tokenVersion int overflow** — 20억 회 비밀번호 변경 후 overflow. 실질 불가능. long 전환 시 해소 | detect | |
| 77 | Low | UserException.java | notFound(Long id) | **에러 메시지에 사용자 ID 노출** — ID 제거 완료. 일반 메시지로 통일 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 78 | Low | AuthEventListener.java | (전체) | **로그인 실패 시 자동 계정 잠금 없음** — rate limiting 으로 1차 방어. 향후 N회 실패 → 임시 잠금 도입 가능 | detect | |
| 79 | Low | SecurityConfig.java | CSP | **CSP `style-src 'unsafe-inline'`** — Tailwind/shadcn 호환 필요. CSS injection 으로 데이터 유출 가능성. 향후 nonce 기반 전환 검토 | detect | |
| 80 | Informational | PostService.java | getFeed | **피드 N+1 쿼리 가능성** — Post → Author lazy loading 으로 DoS 증폭 벡터. EntityGraph 또는 fetch join 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |

## Review #5 — 인증/인가 설계 권장사항

> `docs/auth-design.md` 의 단계별 로드맵에서 도출한 후속 작업 항목. 현재 구현(Phase 0)은 모두 완료되었으며,
> 아래 항목은 운영 안정성·보안·확장성 강화를 위한 권장 작업이다. Code Review 발견 이슈와 달리 "문제" 가 아닌 "다음 단계" 에 가깝다.

### Phase 1 — 즉시 권장 (운영 전 필수)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 45 | Recommend | (신규) | 전 영역 | **Rate limiting 도입 (Bucket4j 등)** — Bucket4j IP 기반 필터. POST /api/auth/login, POST /api/user 분당 10회 제한 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 46 | Recommend | UserService.java | changePassword | **비밀번호 변경 흐름** — changePassword(id, currentRaw, newRaw). 현재 비밀번호 재확인 + tokenVersion bump | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 47 | Recommend | User.java, AuthUser.java | tokenVersion | **token version 컬럼** — User.tokenVersion + AuthUser 보관 + me() 에서 비교. 불일치 시 세션 무효화 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 48 | Recommend | application-prod.yaml | (신규) | **운영 cookie 속성** — secure, http-only, same-site lax, timeout 30m. prod 프로필 분리 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 49 | Recommend | AuthEventListener.java | (신규) | **인증 audit log** — @EventListener 로 Success/Failure/Logout 이벤트 audit 로깅 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 50 | Recommend | SecurityConfig.java | requiresChannel | **HTTPS 강제** — prod 프로필에서만 requiresSecure(). dev 는 HTTP 허용 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 51 | Recommend | SecurityConfig.java | CSRF | **XorCsrfTokenRequestAttributeHandler** — BREACH 압축 공격 완화 적용 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |
| 52 | Recommend | SecurityConfig.java | headers | **Content-Security-Policy** — default-src 'self'; script-src 'self' 적용 | 검증 및 소스 재 리뷰 완료 | 2026-04-12 |

### Phase 2 — SNS 기능 확장 시

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 53 | Recommend | User.java, SecurityConfig.java | (신규) | **Role enum 도입** — `Role { USER, ADMIN }`. `AuthUser.getAuthorities()` 가 `[ROLE_USER]` 또는 `[ROLE_USER, ROLE_ADMIN]` 반환. `@EnableMethodSecurity` 활성화 + `/api/admin/**` hasRole("ADMIN") | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 54 | Recommend | (신규) | EmailVerification 도메인 | **이메일 인증 흐름** — 가입 후 단기 signed JWT(24h, jti) 를 메일로 발송. 검증 시 jti 1회 사용 처리. JWT 가 자연스럽게 적합한 첫 시나리오 | detect | |
| 55 | Recommend | (신규) | PasswordReset 도메인 | **비밀번호 재설정 흐름** — 단기 JWT(30m, jti, 1회 사용) + 새 비밀번호 입력 폼. token version 과 연계하여 재설정 시 모든 세션 무효화 | detect | |
| 56 | Recommend | UserService.java | (신규) | **Remember-Me ("로그인 유지")** — Spring Security `rememberMe()` DSL + `PersistentTokenRepository`. DB 에 토큰 저장 | detect | |

### Phase 3 — 다중 인스턴스 / 다중 클라이언트

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 57 | Recommend | (신규) | build.gradle.kts + 설정 | **Spring Session + Redis 도입** — 세션 외부화로 sticky session 불필요. `FindByIndexNameSessionRepository` 로 사용자별 세션 일괄 invalidate 가능. 다중 인스턴스/zero-downtime 배포 시점 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 58 | Recommend | (신규) | 2FA 도메인 | **2FA (TOTP)** — Google Authenticator 호환. SecuredAction 별 옵션화 | detect | |
| 59 | Recommend | SecurityConfig.java | OAuth2 | **OAuth2 소셜 로그인** — `oauth2Login()` DSL + Google/GitHub provider. 자체 가입과 병행 | detect | |

### Phase 4 — 모바일 / 외부 API

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 60 | Recommend | (신규) | TokenAuth 도메인 | **JWT 발급 엔드포인트** (`/api/auth/token`) — 모바일 전용. access(15m) + refresh(14d). refresh 회전. 웹 세션 인증과 병행 | detect | |
| 61 | Recommend | (신규) | ApiKey 도메인 | **API key 발급/회전** — 외부 자동화 클라이언트 / 서드파티 통합용. 어드민 콘솔에서 발급/취소 | detect | |
| 62 | Recommend | SecurityConfig.java | OAuth2 client_credentials | **서드파티 시스템 인증** — OAuth2 client credentials grant. 인간 사용자와 분리 | detect | |

### 권장 우선순위 (Phase 1 내)

1. **#45 Rate limiting** — 무차별 대입 차단, 가장 시급
2. **#47 Token version + #46 비밀번호 변경 흐름** — 자격증명 보호 (함께 도입 권장)
3. **#48 운영 cookie 속성 + #50 HTTPS 강제** — 배포 직전 필수
4. **#49 인증 audit log** — 침해 사고 분석 기반

## Review #8 — 전체 소스 리뷰 (2026-04-13)

> Redis 세션 관리 구현(Phase 1.5) 완료 후 전체 소스 리뷰. 기존 #57(Spring Session + Redis) 해결 확인. 신규 11건 발견.

### Critical (must fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 81 | Critical | PostResponse.java | :10 | **PostResponse.from(Post) LazyInitializationException** — `post.getAuthor()` 가 lazy proxy. `PostService.create()`/`getById()` 에 `@Transactional` 없어 persistence context 닫힌 후 Controller 에서 author 접근 시 예외 발생. `getReferenceById` → `findById` 교체 + fetch join 추가 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 82 | Critical | RateLimitFilter.java | :43 | **emailBuckets ConcurrentHashMap 미사용 dead code** — 선언 + eviction 코드 있으나 실제 populate 로직 없음. `EMAIL_REQUESTS_PER_MINUTE` 상수도 dead. dead code 제거 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |

### Warning (should fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 83 | Warning | AuthController.java | :33-35 | **me() 에서 authUser null 방어 없음** — SecurityConfig 의 `authenticated()` 로 차단되지만, 방어적 null check 부재. 설정 변경 시 NPE 위험 | detect | |
| 84 | Warning | User.java | :64 | **update() 가 nickname+password 항상 동시 요구** — changePassword 에서 기존 nickname 을 다시 전달하는 불필요한 결합. `changePassword(String encodedPassword)` 분리 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 85 | Warning | RestAuthenticationFilter.java | :35-36 | **Content-Type 비교가 exact match** — `application/json; charset=utf-8` 등 변형을 거부. `startsWith("application/json")` 또는 `MediaType.parseMediaType()` 사용 권장 | detect | |
| 86 | Warning | SecurityConfig.java | :39 | **MAX_SESSIONS_PER_USER = 3 하드코딩** — `@ConfigurationProperties` 로 외부화하여 배포 없이 변경 가능하도록 권장 | detect | |
| 87 | Warning | AbsoluteSessionTimeoutFilter.java | :23 | **ABSOLUTE_TIMEOUT = 24h 하드코딩** — application.yaml 프로퍼티로 외부화 권장 | detect | |
| 88 | Warning | RateLimitFilter.java | :34-40 | **IP_REQUESTS_PER_MINUTE, BUCKET_EXPIRY 하드코딩** — 운영 환경별 조정 필요. 프로퍼티 외부화 권장 | detect | |
| 89 | Warning | UserService.java | :77 | **changePassword() 에서 currentRawPassword null/blank 미검증** — `passwordEncoder.matches(null, ...)` 호출 시 NPE 가능 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 90 | Warning | RateLimitFilter.java | :85-93 | **evictExpiredBuckets() 가 전체 맵 clear** — 10,000 엔트리 초과 시 합법 사용자도 rate limit 상태 초기화. Caffeine 캐시 등 개선 권장 | detect | |

### Suggestion (consider improving)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 91 | Suggestion | SecurityConfig.java, SessionConfig.java | :43, :26 | 수동 생성자 주입 대신 `@RequiredArgsConstructor` 사용으로 컨벤션 통일 가능 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 92 | Suggestion | UserDetailsServiceImpl.java | :26, :35 | 더미 비밀번호 문자열 상수 `DUMMY_RAW_PASSWORD` 로 추출 완료. UsernameNotFoundException 메시지에서 email 제거 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |
| 93 | Suggestion | PostService.java | :5 | `PostService` → `UserRepository` 크로스 도메인 의존 Javadoc 문서화. 향후 확장 시 분리 검토 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |

## Review #9 — 해커 관점 보안 리뷰 2차 (2026-04-13)

> Redis 세션 구현 후 침투 테스터 관점 재리뷰. 공격 체인 중심 분석. 신규 13건 발견 (기존 이슈와 일부 중복은 별도 번호 부여).

### Critical

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 94 | Critical | UserUpdateRequest / UserService.update | UserController:70 | **`PUT /api/user/{id}` 가 현재 비밀번호 확인 없이 password 변경 허용** — 세션 탈취 시 현재 비밀번호 불필요 + tokenVersion 미증가로 피해자 세션 무효화 안됨. 전용 `/password` 엔드포인트 우회 백도어 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |

### High

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 95 | High | RateLimitFilter.java | :85-93 | **ipBuckets.clear() 전체 리셋 공격** — 10,001개 고유 IP 요청으로 모든 사용자 rate limit 초기화 + 자신의 brute force 재개 가능. Caffeine 캐시로 개별 eviction 교체 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 96 | High | RateLimitFilter.java | :43, :61-66 | **이메일 기반 rate limiting 미구현** — IP 로테이션(프록시 50개)만으로 분당 1,000회 특정 계정 공격 가능. dead code 제거 완료, 이메일 기반은 인증 실패 이벤트 기반으로 향후 구현 예정 (TODO) | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |

### Medium

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 97 | Medium→High | AuthController / SecurityConfig | 전체 | **tokenVersion 매 요청 미검증** — #123으로 통합. `TokenVersionFilter` 구현 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 98 | Medium | application.yaml | :5 | **dev 프로필이 기본값** — #128로 통합. base yaml에 cookie 보안 기본값 설정 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 99 | Medium | UserController, UserResponse | GET /api/user | **인증 없이 전체 사용자 이메일 수집** — `GET /api/user?size=1000` 으로 모든 등록 이메일 열거 가능. 공개 응답을 `UserSummaryResponse(id, nickname)` 으로 분리하여 email/role 노출 차단 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 100 | Medium | PageableDefault | Controller 전체 | **Pageable size 무제한** — `spring.data.web.pageable.max-page-size=100` 설정 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 101 | Medium | SecurityConfig.java | CSRF ignore | **H2 콘솔 CSRF 면제가 프로필 무관** — dev(기본값)에서 CSRF 없이 SQL 실행 가능. 개발자 브라우저 대상 CSRF 공격으로 DB 탈취 (cf. #74 후속) | detect | |
| 102 | Medium | AuthUser.java | password 필드 | **Redis 세션에 비밀번호 해시 직렬화** — `AuthUser` 에서 password 필드 완전 제거, `getPassword()` → null | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 103 | Medium | AuthEventListener.java | 계정 잠금 | **계정 단위 잠금 없음** — IP 로테이션 시 무제한 시도. N회 실패 → 임시 잠금 + Redis 기반 실패 횟수 추적 필요 (cf. #78 후속) | detect | |

### Low

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 104 | Low | RestAuthenticationFilter.java | :35-36 | **Content-Type exact match** — `application/json; charset=utf-8` (공백 포함) 거부. MediaType 파싱으로 교체 완료 (cf. #85) | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 105 | Low | AuthEventListener.java | audit log | **감사 로그 개행 삽입(log injection)** — `sanitize()` 메서드로 `\r\n\t` → `_` 치환 적용 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 106 | Low | GlobalExceptionHandler.java | (전체) | **catch-all 핸들러 없음** — `@ExceptionHandler(Exception.class)` 추가 + `server.error.include-stacktrace=never` | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |

## Review #10 — 전체 소스 리뷰 (2026-04-13)

> Critical/Warning 전건 수정 후 전체 소스 재리뷰. 이전 이슈 #83~#90, #94, #95, #96, #104 모두 RESOLVED 확인. Critical 0건. 신규 Warning 5건, Suggestion 2건 발견.

### Warning (should fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 107 | Warning | PostController.java | :72 | **requireAuth()가 IllegalStateException throw** — GlobalExceptionHandler에 미처리되어 500 응답. `AccessDeniedException` 으로 교체 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 108 | Warning | SecurityConfig.java | :42 | **수동 생성자 주입** — `@RequiredArgsConstructor` 로 교체 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 109 | Warning | SessionConfig.java | :26 | **수동 생성자 주입** — `@RequiredArgsConstructor` 로 교체 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 110 | Warning | PostService.java | :18-20 | **create()에서 content null/blank, authorId null 미검증** — Service 레벨 검증 추가 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 111 | Warning | PostService.java | :20 | **authorId 미존재 시 PostException.invalidField("author") 오해의 소지** — `PostException.authorNotFound(authorId)` 로 교체 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |

### Suggestion (consider improving)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 112 | Suggestion | AuthController.java | :35, :43 | `ResponseEntity.status(401)` 매직 넘버 → `HttpStatus.UNAUTHORIZED` 로 교체 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 113 | Suggestion | RateLimitFilter.java | :41 | Caffeine `expireAfterAccess(10분)` 하드코딩 → `RateLimitProperties.bucketExpiry` 로 외부화 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |

## Review #11 — 전체 소스 리뷰 (2026-04-14)

> Review #10 Warning/Suggestion 전건 해결 확인. #53 Role enum, #99 UserSummaryResponse 등 8건 RESOLVED. Critical 0건. 신규 Warning 4건, Suggestion 5건 발견.

### Warning (should fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 114 | Warning | UserService.java | :36-38 | **DataIntegrityViolationException catch가 email만 가정** — `existsByNickname` 선행 체크 + catch 분기에서 재확인 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 115 | Warning | PostService.java | :40 | **update() content Service 레벨 검증 누락** — content null/blank 검증 추가 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 116 | Warning | PostService.java | :40 | **update()/delete() requesterId null 미검증** — requesterId null 체크 추가 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 117 | Warning | SecurityConfig, AbsoluteSessionTimeoutFilter, RateLimitFilter | 다수 | **JSON 에러 응답 중복** — `FilterResponseUtils.writeJsonError()` 유틸 추출 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |

### Suggestion (consider improving)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 118 | Suggestion | PostCreateRequest, PostUpdateRequest | :7 | `@Size(max = Post.MAX_CONTENT_LENGTH)` 로 Entity 상수 참조 교체 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |
| 119 | Suggestion | RestAuthSuccessHandler.java | :37-47 | `UserResponse.from(AuthUser)` 오버로드 추가 + inline Map 제거 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |
| 120 | Suggestion | AuthController.java | :48 | `UserException` catch 범위 축소 — `NOT_FOUND` 만 catch, 나머지는 GlobalExceptionHandler 위임 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |
| 121 | Suggestion | UserService.java | :77 | 동일 비밀번호 변경 차단 — `currentRaw.equals(newRaw)` 검증 + `UserException.samePassword()` 추가 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |
| 122 | Suggestion | User.java | :42 | `this.tokenVersion = 0` 명시적 초기화 추가 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |

## Review #12 — 해커 관점 보안 리뷰 3차 (2026-04-14)

> Warning 전건 수정 후 침투 테스터 관점 재리뷰. 기존 수정사항(IDOR, XFF, Caffeine, password 분리, UserSummaryResponse, Role 등) 확인됨. 신규 High 1건, Medium 4건, Low 1건 발견 (기존 이슈와 중복분은 참조 표기).

### High

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 123 | High | AuthController / SecurityConfig | 전체 인증 경로 | **tokenVersion 매 요청 미검증** — `TokenVersionFilter` 구현 (Caffeine 캐시 30초 TTL + evict on changePassword) | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |

### Medium

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 124 | Medium | FilterResponseUtils.java | :15 | **문자열 결합 JSON 생성** — ObjectMapper + `Map.of()` 로 안전한 직렬화 교체 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 125 | Medium | UserService.java | register | **회원가입 에러 메시지로 email/nickname 존재 여부 열거** — 공격자가 `POST /api/user` 로 등록 여부 대량 확인 가능. 일반 메시지로 통일 권장 (cf. #72 #99 후속) | detect | |
| 126 | Medium | UserController.java | deleteUser | **계정 삭제 시 Redis 세션 미무효화** — `invalidateAllSessions()` + 현재 세션 무효화 추가 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |
| 127 | Medium | (미구현) | — | **비밀번호 유출 목록 대조 미적용 (Pwned Passwords)** — NIST 800-63B "SHALL". 설계 완료(§9.3)되었으나 미구현. 약한 비밀번호("12345678") 허용 상태 | detect | |

### Low

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 128 | Low | application.yaml | :5 | **dev 프로필 기본값 → cookie 보안 플래그 없이 배포 가능** — base yaml에 secure/httpOnly/sameSite 기본값 설정 + dev에서 secure=false 완화 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |

## Review #13 — 회원 흐름 전수 검증 (2026-04-14)

> 회원가입/로그인/정보 수정/비밀번호 변경/탈퇴/조회 6개 흐름 end-to-end 검증. Critical 1건 + Minor 1건 발견 즉시 수정.

### Critical (즉시 수정)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 129 | Critical | AuthUser.java / UserDetailsServiceImpl.java | :42, :38 | **로그인 불가** — `AuthUser.getPassword()` → null 반환으로 `DaoAuthenticationProvider` 비밀번호 검증 항상 실패. password 필드 제거(#102) 시 인증 흐름 고려 누락. `transient` 키워드로 Redis 직렬화 제외 + 인증 시점에만 해시 보유하도록 수정 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |

### Warning (수정 완료)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 130 | Warning | UserService.java | :71-76 | **nickname 수정 시 중복 검증 누락** — `update()` 에 `existsByNickname` 체크 없어 중복 nickname 시 500. `register()` 패턴과 불일관. 선행 체크 추가 | 검증 및 소스 재 리뷰 완료 | 2026-04-14 |

### Suggestion (추가 권장)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 131 | Suggest | UserService.java | :84 | 동일 비밀번호 변경 차단 — #121 과 함께 해결 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |
| 132 | Suggest | ChangePasswordRequest.java | currentPassword | `@Size(min=8, max=64)` 추가 — fast-fail 적용 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |
| 133 | Suggest | TokenVersionFilter.java | 캐시 | 다중 인스턴스 캐시 불일치 — Javadoc 에 주의사항 문서화 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-15 |

## Review #14 — Follow 도메인 + 민감 정보 감사 (2026-04-16)

> Follow 도메인(Entity/Service/FollowCount/Retryable/Soft Delete) 구현 + 민감 정보 4관점(Cache/Cookie/Token/Error) 감사. Critical 3건, Warning 8건, Suggestion 6건 발견.

### Critical (must fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 134 | Critical | FollowService.java | :65-79 | **중복 삽입 race → 500 전파** — try/catch (`DataIntegrityViolationException`) 로 `alreadyFollowing` (409) 변환 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-16 |
| 135 | Critical | FollowService.updateFollowCounts | :174-194 | **UPDATE 0 rows affected silent fail** — `ensureFollowCountExists` 로 findOrCreate 패턴 적용. UPDATE 반환 타입 `int` 로 변경 | 검증 및 소스 재 리뷰 완료 | 2026-04-16 |
| 136 | Critical | FollowService.unfollow | :97-131 | **selfFollow 검증 누락** — `followerId.equals(followingId)` 체크 추가로 follow 와 대칭 | 검증 및 소스 재 리뷰 완료 | 2026-04-16 |

### Warning (should fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 137 | Warning | FollowCountResponse.java | :3 | **API/도메인 네이밍 불일치** — DTO는 `followingCount`, 엔티티/서비스는 `followeesCount`. 한쪽으로 통일 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 138 | Warning | FollowController.java | :30-43 | `@AuthenticationPrincipal AuthUser authUser` null 방어 누락 — `PostController.requireAuth` 패턴과 대칭 미구현 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 139 | Warning | FollowException/UserException/PostException | 팩토리 전체 | **파라미터 미사용** — id/email/nickname 파라미터가 메시지에 미포함이라 unused. 로깅 컨텍스트를 위해 내부 필드로 보관 후 감사 로그에 활용 권장 | detect | |
| 140 | Warning | GlobalExceptionHandler.java | :38-43 | **AccessDeniedException → 500** — Spring Security `AccessDeniedException` 이 catch-all로 500 처리. 403 전용 핸들러 또는 `AccessDeniedHandler` 등록 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 141 | Warning | AuthEventListener.java | :30-34 | **로그인 실패 email 원문 audit 로그** — sanitize 만으로는 부족. 로그 유출 시 시도된 email 리스트 노출. 해시/부분 마스킹(`u***@example.com`) 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 142 | Warning | FollowService.follow | :65 | `findByFollowerAndFollowing` fetch 전략 불명확 — 현재는 트랜잭션 내 로딩된 User 프록시라 문제 없으나 `@EntityGraph` 로 명시 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 143 | Warning | UserService.delete | :110-114 | **계정 삭제 시 관련 레코드 방치** — Follow(양방향), FollowCount, Post 남음. FK 없으므로 dangling reference. cascade 처리 또는 User soft delete 전환 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |
| 144 | Warning | AuthUser.java | :32 | `tokenVersion` `@Getter` 노출 — 현재 `UserResponse` 에 미포함이나 방어적으로 `@JsonIgnore` 부착 권장 | 검증 및 소스 재 리뷰 완료 | 2026-04-13 |

### Suggestion (consider improving)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 145 | Suggestion | src/main/resources/http/ | (신규) | Follow/Post/Auth 용 `follow.sh`, `post.sh`, `auth.sh` 부재 — SKILL.md 의 통합 테스트 스크립트 규칙 미준수 | detect | |
| 146 | Suggestion | UserController.java | :42 | TokenVersionFilter 풀 FQN 사용 — import 정리 권장 | detect | |
| 147 | Suggestion | UserController.java | :144-167 | `invalidateOtherSessions/All` — Controller 에 인프라(Redis) 로직. `SessionCleanupService` 로 분리 권장 | detect | |
| 148 | Suggestion | FollowService | :131-155 | `getFollowers/getFollowings/isFollowing` 가 User 조회 2회 + Follow 조회 1회 = 3쿼리. userId 직접 쿼리로 통합하면 1쿼리 | detect | |
| 149 | Suggestion | FollowRepository | :22-30 | `findActiveFollowersByUser/Followings` 의 명시적 `countQuery` 미지정 — 자동 생성 의존 | detect | |
| 150 | Suggestion | ErrorType.java | :11-15 | `UNAUTHORIZED`, `INTERNAL_SERVER_ERROR` 확장 포인트 부족 | detect | |

### 민감 정보 감사 결과 (Cache / Cookie / Token / Error)

| 카테고리 | 항목 | 상태 |
|---------|------|------|
| **Cache** | Caffeine (TokenVersion/RateLimit): userId+int, ip+bucket만 저장 | ✅ 안전 |
| **Cache** | UserDetailsService 더미 해시 (고정값) | ✅ 안전 |
| **Redis** | AuthUser: password `transient` 처리로 해시 미저장 | ✅ 안전 |
| **Cookie** | Session: secure/httpOnly/sameSite=lax (dev만 secure=false) | ✅ 안전 |
| **Cookie** | CSRF: XorCsrfTokenHandler (BREACH 방어) | ✅ 안전 |
| **Token** | `UserResponse` 에 tokenVersion 미포함 | ✅ 안전 (추가 방어 #144) |
| **Error** | include-stacktrace=never + 일반화된 메시지 | ✅ 안전 |
| **Error** | 로그인 실패 email 원문 audit | ⚠️ 개선 필요 (#141) |
| **Error** | AccessDeniedException 500 처리 | ⚠️ 수정 필요 (#140) |

### 상태값 정의

| 상태 | 설명 |
|------|------|
| detect | 이슈 최초 발견 |
| 수정 진행중 | 수정 작업 진행 중 |
| 수정 완료 | 수정 작업 완료 |
| 검증 및 소스 재 리뷰 완료 | 수정 검증 및 재 리뷰 완료 |

> **상태값 메모 (Recommend 구분)**: Review #5 의 항목들은 "문제(bug/anti-pattern)" 가 아니라 "권장 다음 단계(roadmap)" 이므로,
> `detect` 는 "권장 등록됨" 의 의미로 사용한다. 작업 착수 시 `수정 진행중`, 완료 시 `검증 및 소스 재 리뷰 완료` 로 진행한다.
