# Code Review History — 2026-04

## Review #1

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 1 | Critical | UserService.java | :14 | `register()`에서 `existsByEmail`과 `save` 사이 race condition — 동시 요청 시 중복 저장 가능. `DataIntegrityViolationException` 미처리로 500 에러 발생 | 검증 및 소스 재 리뷰 완료 | 2026-04-09 |
| 2 | Critical | User.java | :33 | 비밀번호 평문 저장 — Argon2 암호화 적용 완료 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 3 | Warning | User.java | :37-39 | `update()` 메서드에서 nickname, password에 null/빈 문자열 검증 없음 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 4 | Warning | UserCreateRequest.java | :5 | 요청 DTO에 `@NotBlank` 등 Bean Validation 미적용 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 5 | Warning | UserUpdateRequest.java | :3 | 요청 DTO에 Bean Validation 미적용 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 6 | Warning | GlobalExceptionHandler.java | :12-15 | `UserException`만 처리 — 공통 부모 예외 클래스 도입 필요 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 7 | Suggestion | UserRepository.java | :8 | `findByEmail()` 미사용 메서드 — 불필요 시 제거 고려 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |
| 8 | Suggestion | UserController.java | :39-42 | `getUsers()` 전체 조회 — 페이징(`Pageable`) 도입 고려 | 검증 및 소스 재 리뷰 완료 | 2026-04-11 |

## Review #2

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 9 | Warning | User.java | :10,38-40 | Entity가 `PasswordEncoder`(Spring Security)를 직접 의존 — 도메인 레이어 독립성 저해. Service에서 인코딩 후 값을 전달하는 방식으로 변경 권장 | detect | |
| 10 | Warning | UserService.java | :21,40 | `register()`는 Entity에 위임, `update()`는 Service에서 직접 인코딩 — 암호화 패턴 비일관. update()는 Service 직접 인코딩으로 통일되었으나 register()는 여전히 Entity 위임 방식 유지 | 수정 진행중 | |

## Review #3

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 11 | Warning | UserService.java | :18-28 | `register()` 에 raw 값(email/password/nickname) Service 레벨 방어 검증 없음. Bean Validation 우회 경로(내부 호출)에서 NPE 위험. `update()` 처럼 인코딩/저장 호출 전 raw 값 선검증 필요 | detect | |
| 12 | Warning | User.java | :32-36 | `public User(String, String, String)` 생성자에서 raw 파라미터 null/blank 검증 없이 필드 저장 — Entity 불변식 방어 누락. 생성자에서도 검증 적용 필요 | detect | |
| 13 | Warning | UserService.java | :48-53 | `delete()` 가 `existsById` + `deleteById` 로 DB 2회 접근. 트랜잭션 미적용으로 두 호출 사이 race condition 가능. `findById().ifPresent(repo::delete)` 또는 `@Transactional` 적용 권장 | detect | |
| 14 | Suggestion | GlobalExceptionHandler.java | :14-17 | 모든 `DomainException` 을 400(Bad Request)로 응답. `notFound` 류는 의미상 404 가 적절. `DomainException` 에 HTTP 상태 노출 또는 `NotFoundException` 하위 타입 도입 검토 | detect | |
| 15 | Suggestion | UserException.java | :7-9 | `public UserException(String)` 생성자가 외부에 노출. 정적 팩토리만 허용하도록 생성자를 `private` 로 제한하여 메시지 포맷 일관성 강제 가능 | detect | |
| 16 | Suggestion | UserController.java | :51-56 | `updateUser` 가 DTO 필드를 풀어 service에 전달. `UserUpdateRequest` 에 `toCommand()` / VO 변환 메서드를 두어 Controller-Service 결합도 완화 검토 | detect | |
| 17 | Suggestion | DomainException.java | :6 | `serialVersionUID` 미정의 — `RuntimeException` 직렬화 경고 가능성. 명시 권장 | detect | |
| 18 | Suggestion | PageResponse.java | :7-25 | `content` 가 가변 `List<T>` 로 노출됨. `List.copyOf(...)` 로 감싸 불변성 보장 권장 | detect | |

## Review #4

> 백엔드 인증/Post 도메인 + 프론트엔드 Next.js 16 풀스택 부트스트랩 직후 리뷰. 4월 기존 이슈(#9~#18)는 모두 미해결 상태로 변동 없음.

### Critical (must fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 19 | Critical | SecurityConfig.java | :21-54 | 로그인 시 **세션 고정 공격(Session Fixation) 방어 누락**. `AuthController` 가 수동으로 `authenticate()` + `securityContextRepository.saveContext()` 를 호출하는데, 세션 ID 교체(`request.changeSessionId()` 또는 `SessionAuthenticationStrategy.onAuthentication`)를 수행하지 않음. 로그인 전후 세션 ID 동일 → 공격 가능 | detect | |
| 20 | Critical | SecurityConfig.java | :22 | **CSRF 비활성화 + 쿠키 세션 조합으로 CSRF 공격에 노출**. same-origin 프록시 구조에서 제3자 사이트의 폼 제출도 쿠키가 자동 첨부됨. 최소 `SameSite=Strict` 강제 또는 `CookieCsrfTokenRepository` 도입 필요 | detect | |
| 21 | Critical | PostController.java | :32-34, 52-57, 61-64 | `Authentication` 파라미터 null 방어 없음 → 룰 변경/anonymous 활성화 시 NPE 직행. 추가로 `authentication.getName()` → Service 가 `userService.getByEmail()` 로 1회 더 DB 조회 → 모든 mutation 에서 불필요한 추가 쿼리. `@AuthenticationPrincipal` 로 사용자 ID 직접 주입 권장 | detect | |
| 22 | Critical | frontend/lib/api.ts | :155-165, 178-189 | **에러/스키마 검증 실패 시 response body stream 누수**. `parseError` 의 `response.json()` 실패 catch 경로에서 body 를 텍스트로 fallback 소진하지 않음. `response.clone()` 후 json 시도 + 실패 시 `response.text()` 로 stream 확실히 소진 필요 | detect | |
| 23 | Critical | frontend/features/post/hooks/useFeedQuery.ts, components/FeedList.tsx | useFeedQuery.ts:7, FeedList.tsx:19-35 | (a) `feedQueryKey = ['feed']` 단일 키 → 정렬/사이즈 변경 시 캐시 오염. (b) `useCreatePostMutation` 의 전체 invalidate → 무한쿼리 모든 페이지 refetch 로 네트워크 과부하. (c) IntersectionObserver effect 의 `isFetchingNextPage` deps → 페치 토글마다 disconnect/re-observe 로 연속 스크롤 끊김 | detect | |

### Warning (should fix)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 24 | Warning | AuthController.java | :49-51 | `BadCredentialsException` 만 401 변환. `LockedException`, `DisabledException`, `AccountExpiredException`, `UsernameNotFoundException` 등 다른 `AuthenticationException` 하위는 500 으로 떨어짐. 공통 부모 `AuthenticationException` catch 또는 `GlobalExceptionHandler` 핸들러 추가 필요 | detect | |
| 25 | Warning | AuthController.java | :54-63 | `me()` 가 `getByEmail` 시 사용자가 그 사이 삭제되면 `UserException.notFoundByEmail` → 400 응답. 401 매핑 또는 세션 정리 필요 | detect | |
| 26 | Warning | PostService.java | :32-50 | `update()` 만 `@Transactional` 이고 `delete()` 는 미적용. `delete()` 는 getById + getByEmail + delete 로 3회 DB 접근이 각각 독립 트랜잭션. 단일 트랜잭션으로 묶어야 함 | detect | |
| 27 | Warning | PostRepository.java | :9 | `findAllByOrderByIdDesc` 메서드명에 정렬을 하드코딩 → Controller 가 `@PageableDefault(sort=...)` 로 변경해도 무시됨. 표준 `findAll(Pageable)` 사용으로 충분 | detect | |
| 28 | Warning | PostException.java | :7 | 생성자가 `public` — 이슈 #15 와 동일 패턴이 새 도메인에 전파. 정적 팩토리만 허용하도록 `private` 제한 권장 | detect | |
| 29 | Warning | SecurityConfig.java | :31-32 | `/api/auth/me` 를 permitAll 로 두고 컨트롤러에서 anonymous 수동 체크. `authenticated()` + `AuthenticationEntryPoint` 401 반환이 표준. 현재 방식은 일관성 부족 | detect | |
| 30 | Warning | UserDetailsServiceImpl.java | :18-28 | 존재하지 않는 이메일에 대해 `passwordEncoder.matches` 없이 즉시 throw → **타이밍 공격으로 계정 존재 여부 탐지 가능**. 더미 hash 와 matches 호출로 타이밍 균일화 필요 | detect | |
| 31 | Warning | frontend/lib/api.ts | :129-189 | 제네릭 반환 타입에 zod 의 `infer Out` 사용 → zod v4 의 3-제네릭 ZodType 에서 추론 실패 가능. `z.output<NonNullable<TSchema>>` 사용 권장 (특히 `transform` 결과 타입의 정확성) | detect | |
| 32 | Warning | frontend/lib/api.ts | :155-160 | `AbortError` 도 `ApiError(status=0)` 로 래핑 → TanStack Query 의 cancel/언마운트 흐름과 구분 불가. `e.name === 'AbortError'` 는 rethrow 해야 함 | detect | |
| 33 | Warning | frontend/lib/api.ts | :186 | 스키마 검증 실패 시 `status = response.status`(=200)로 `ApiError` 생성 → `isUnauthorized` 등 플래그와 충돌 여지. 별도 에러 클래스 또는 `status: 0` 권장 | detect | |
| 34 | Warning | frontend/components/providers/QueryProvider.tsx | :14 | `getQueryClient()` 를 컴포넌트 내부 직접 호출. React strict mode/동시 렌더에서 클라이언트 인스턴스 흔들림 위험. `useState(() => getQueryClient())` 패턴 권장 (TanStack Query SSR 가이드) | detect | |
| 35 | Warning | frontend/features/auth/components/LoginForm.tsx, SignupForm.tsx, frontend/features/post/components/PostComposer.tsx | LoginForm:45-49, SignupForm:54-58, PostComposer:43-47 | `setError(field as keyof Values)` 강제 캐스팅 — 백엔드가 알 수 없는 필드명을 응답하면 react-hook-form 이 silent fail 하여 사용자가 에러를 못 봄. 화이트리스트 매핑 + 미지원 필드는 `setError('root', ...)` 로 fallback 필요 | detect | |
| 36 | Warning | frontend/features/auth/components/SignupForm.tsx | :46-52 | 가입 → 자동 로그인 연쇄 호출. 가입 성공 후 로그인만 실패 시 사용자 혼란. 가입 409 에러와 로그인 401 에러가 동일 필드로 매핑될 여지. 단계 분리 + 메시지 차별화 필요 | detect | |
| 37 | Warning | frontend/components/layout/Header.tsx | :20-41 | 초기 렌더 시 `useCurrentUser` 가 fetch 완료 전까지 비로그인 UI 가 깜빡일 가능성. `HydrationBoundary` 로 prefetch 전달하여 SSR 시점에 상태 결정 권장 | detect | |
| 38 | Warning | frontend/features/post/components/FeedItem.tsx | :8-15, 17 | `'use client'` 선언 없음. Server Component 로 직접 렌더되면 `Date.now()` 가 SSR 시각 → hydration mismatch ("방금 전" → "2분 전"). 파일 단위 보장 위해 `'use client'` 명시 또는 `suppressHydrationWarning` 필요 | detect | |
| 39 | Warning | frontend/features/post/components/ComposerSection.tsx, components/layout/Header.tsx | ComposerSection:10, Header:10 | 동일 `useCurrentUser` 두 곳에서 호출 — staleTime 만료 후 중복 fetch 가능. 레이아웃 상단 1회 호출 후 Context 전달 또는 prefetch 로 통일 권장 | detect | |
| 40 | Warning | frontend/lib/env.ts | :24-27 | 모듈 로드 시점에 `throw` → 잘못된 환경변수 시 Next.js 빌드/부팅 자체 실패. 빌드 타임 검증 실패 리스크 인지 필요 | detect | |
| 41 | Warning | SecurityConfig.java | :51 | `frameOptions.sameOrigin()` 를 H2 콘솔 위해 전역 허용 — 운영 환경에서 clickjacking 가능. dev profile 에서만 허용하도록 분리 필요 | detect | |

### Suggestion (consider improving)

| # | 구분 | 파일 | 위치 | 이슈 내용 | 상태 | 해결 일자 |
|---|------|------|------|-----------|------|-----------|
| 42 | Suggestion | frontend/features/post/types.ts | :14 | `createdAt: z.string().transform((s) => new Date(s))` 가 유효성 검사 없이 Date 생성 → `Invalid Date` 가 통과. `z.string().datetime()` 선행 검증 권장 | detect | |
| 43 | Suggestion | UserDetailsServiceImpl.java | 위치 | Spring Security 어댑터인 `UserDetailsServiceImpl` 이 `domain/user/` 에 위치. `domain/user/security/` 또는 `config/` 로 이동하여 레이어 의도 명확화 | detect | |
| 44 | Suggestion | frontend/features/post/components/PostComposer.tsx | :68-72 | 글자 수 카운터는 `content.length`, 게시 버튼 disable 은 `content.trim().length === 0` 사용 → 공백만 입력 시 카운터 표시되지만 버튼 비활성으로 UX 혼동 | detect | |

### 상태값 정의

| 상태 | 설명 |
|------|------|
| detect | 이슈 최초 발견 |
| 수정 진행중 | 수정 작업 진행 중 |
| 수정 완료 | 수정 작업 완료 |
| 검증 및 소스 재 리뷰 완료 | 수정 검증 및 재 리뷰 완료 |
